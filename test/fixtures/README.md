This directory contains tiny, generated fixtures used by the test suite.

- `dummy.pdf` is a *fake* PDF header file (not a real document). It exists only
  to provide a stable file path + extension for the attachments acceptance test.
  It contains no proprietary or sensitive content.
